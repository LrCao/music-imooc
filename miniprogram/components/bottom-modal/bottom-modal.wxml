<view class="modal" hidden="{{!modalShow}}">
  <view class="panel">
    <i class="iconfont icon-chenghao" bind:tap="onClose" />
    <!-- slot 插槽 -->
    <slot name="modal-content"></slot>
  </view>
</view>
